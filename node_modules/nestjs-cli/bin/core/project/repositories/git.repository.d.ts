import { Repository } from '../../../common/project/interfaces/repository.interface';
export declare class GitRepository implements Repository {
    private _remote;
    private _destination;
    private logger;
    constructor(_remote: string, _destination: string);
    clone(): Promise<void>;
}
