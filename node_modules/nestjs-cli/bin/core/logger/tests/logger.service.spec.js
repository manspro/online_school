"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const chai_1 = require("chai");
const logger_service_1 = require("../logger.service");
describe('LoggerService', () => {
    describe('#setLogger()', () => {
        it('should return the set logger after setLogger() called', () => {
            logger_service_1.LoggerService.setLogger(console);
            chai_1.expect(logger_service_1.LoggerService.getLogger()).to.be.deep.equal(console);
        });
    });
    describe('#getLogger()', () => {
        it('should return console logger if no logger is set', () => {
            chai_1.expect(logger_service_1.LoggerService.getLogger()).to.be.deep.equal(console);
        });
    });
});
