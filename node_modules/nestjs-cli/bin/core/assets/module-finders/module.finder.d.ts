import { ModuleFinder } from '../../../common/asset/interfaces/module.finder.interface';
export declare class ModuleFinderImpl implements ModuleFinder {
    private MODULE_FILENAME_REGEX;
    findFrom(origin: string): Promise<string>;
    private findIn(dirname);
}
