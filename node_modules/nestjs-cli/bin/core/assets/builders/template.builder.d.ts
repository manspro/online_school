import { Template } from '../../../common/asset/interfaces/template.interface';
import { Replacer } from '../../../common/asset/interfaces/replacer.interface';
export declare class TemplateBuilder {
    private _filename;
    private _replacer;
    addFilename(filename: string): TemplateBuilder;
    addReplacer(replacer: Replacer): TemplateBuilder;
    build(): Template;
}
