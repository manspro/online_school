import { AssetEnum } from '../../../common/asset/enums/asset.enum';
export declare class FileNameBuilder {
    private _name;
    private _asset;
    private _isTest;
    private _extension;
    addName(name: string): FileNameBuilder;
    addAsset(asset: AssetEnum): FileNameBuilder;
    addTest(isTest: boolean): FileNameBuilder;
    addExtension(extension: string): FileNameBuilder;
    build(): string;
    private mapTest();
    private mapAsset();
    private mapName();
    private isPath();
    private mapNameFromPath();
}
