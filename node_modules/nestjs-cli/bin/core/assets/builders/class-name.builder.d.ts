import { AssetEnum } from '../../../common/asset/enums/asset.enum';
export declare class ClassNameBuilder {
    private _name;
    private _asset;
    constructor();
    addName(name: string): ClassNameBuilder;
    addAsset(asset: AssetEnum): ClassNameBuilder;
    build(): string;
    private mapAsset();
    private mapName();
    private isPath();
    private mapNameFromPath();
    private capitalize(name);
}
