"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const template_builder_1 = require("../template.builder");
const chai_1 = require("chai");
describe('TemplateBuilder', () => {
    let builder;
    beforeEach(() => builder = new template_builder_1.TemplateBuilder());
    describe('#addFilename()', () => {
        it('should return the builder instance', () => {
            const filename = 'filename';
            chai_1.expect(builder.addFilename(filename)).to.be.equal(builder);
        });
    });
    describe('#addReplacer()', () => {
        it('should return the builder instance', () => {
            const replacer = {};
            chai_1.expect(builder.addReplacer(replacer)).to.be.equal(builder);
        });
    });
    describe('#build()', () => {
        it('should return the expected template', () => {
            const filename = 'filename';
            const replacer = {};
            chai_1.expect(builder
                .addFilename(filename)
                .addReplacer(replacer)
                .build()).to.be.deep.equal({
                filename: filename,
                replacer: replacer
            });
        });
    });
});
