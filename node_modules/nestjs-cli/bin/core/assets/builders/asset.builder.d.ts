import { Asset } from '../../../common/asset/interfaces/asset.interface';
import { Template } from '../../../common/asset/interfaces/template.interface';
export declare class AssetBuilder {
    private _filename;
    private _className;
    private _template;
    addFilename(filename: string): AssetBuilder;
    addClassName(className: string): AssetBuilder;
    addTemplate(template: Template): AssetBuilder;
    build(): Asset;
}
