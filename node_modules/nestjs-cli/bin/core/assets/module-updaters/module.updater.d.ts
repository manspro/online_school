import { ModuleUpdater } from '../../../common/asset/interfaces/module.updater.interface';
import { AssetEnum } from '../../../common/asset/enums/asset.enum';
export declare class ModuleUpdaterImpl implements ModuleUpdater {
    private finder;
    private logger;
    update(filename: string, className: string, asset: AssetEnum): Promise<void>;
}
