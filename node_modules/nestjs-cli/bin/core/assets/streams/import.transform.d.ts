/// <reference types="node" />
import { Transform } from 'stream';
export declare class ImportTransform extends Transform {
    private className;
    private dirname;
    private template;
    constructor(className: string, dirname: string);
    _transform(chunk: any, encoding: any, done: any): void;
}
