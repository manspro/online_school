/// <reference types="node" />
import { Transform } from 'stream';
import { AssetEnum } from '../../../common/asset/enums/asset.enum';
export declare class MetadataTransform extends Transform {
    private className;
    private asset;
    private MODULE_METADATA_REGEX;
    constructor(className: string, asset: AssetEnum);
    _transform(chunk: any, encoding: any, done: any): void;
    private update(metadata);
    private updateComponents(metadata);
    private updateControllers(metadata);
}
