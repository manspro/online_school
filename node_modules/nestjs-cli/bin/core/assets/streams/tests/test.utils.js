"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const stream_1 = require("stream");
class BufferedReadable extends stream_1.Readable {
    constructor(buffer) {
        super();
        this.buffer = buffer;
        this.isRead = false;
    }
    _read() {
        if (!this.isRead) {
            this.push(this.buffer);
            this.isRead = !this.isRead;
        }
        else {
            this.push(null);
        }
    }
}
exports.BufferedReadable = BufferedReadable;
class BufferedWritable extends stream_1.Writable {
    constructor() {
        super();
        this.buffer = '';
    }
    _write(chunk, encoding, done) {
        this.buffer += chunk.toString();
        done();
    }
    getBuffer() {
        return Buffer.from(this.buffer);
    }
}
exports.BufferedWritable = BufferedWritable;
