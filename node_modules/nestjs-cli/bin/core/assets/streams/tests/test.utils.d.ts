/// <reference types="node" />
import { Readable, Writable } from 'stream';
export declare class BufferedReadable extends Readable {
    private buffer;
    private isRead;
    constructor(buffer: Buffer);
    _read(): void;
}
export declare class BufferedWritable extends Writable {
    private buffer;
    constructor();
    _write(chunk: any, encoding: any, done: any): void;
    getBuffer(): Buffer;
}
