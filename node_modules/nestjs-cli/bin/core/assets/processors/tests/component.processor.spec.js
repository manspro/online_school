"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const asset_generator_1 = require("../../generators/asset.generator");
const sinon = require("sinon");
const path = require("path");
const component_processor_1 = require("../component.processor");
const module_updater_1 = require("../../module-updaters/module.updater");
describe('ComponentProcessor', () => {
    const name = 'name';
    const extension = 'ts';
    let sandbox;
    beforeEach(() => sandbox = sinon.sandbox.create());
    afterEach(() => sandbox.restore());
    let processor;
    beforeEach(() => processor = new component_processor_1.ComponentProcessor(name, extension));
    let generateStub;
    let updateStub;
    beforeEach(() => {
        generateStub = sandbox.stub(asset_generator_1.AssetGenerator.prototype, 'generate').callsFake(() => Promise.resolve());
        updateStub = sandbox.stub(module_updater_1.ModuleUpdaterImpl.prototype, 'update').callsFake(() => Promise.resolve());
    });
    describe('#process()', () => {
        const assets = [
            {
                filename: path.join(process.cwd(), 'src/app/modules/', 'name/name.service.ts'),
                className: 'NameService',
                template: {
                    filename: path.resolve(__dirname, '../../../../assets/ts/component/component.ts.template'),
                    replacer: {
                        __CLASS_NAME__: 'NameService'
                    }
                }
            },
            {
                filename: path.join(process.cwd(), 'src/app/modules/', 'name/name.service.spec.ts'),
                className: 'NameService',
                template: {
                    filename: path.resolve(__dirname, '../../../../assets/ts/component/component.spec.ts.template'),
                    replacer: {
                        __CLASS_NAME__: 'NameService',
                        __IMPORT__: 'name.service.ts'
                    }
                }
            }
        ];
        it('should generate component assets', () => {
            return processor.process()
                .then(() => {
                sinon.assert.calledWith(generateStub, assets[0]);
                sinon.assert.calledWith(generateStub, assets[1]);
            });
        });
        it('should update the nearest parent module', () => {
            return processor.process()
                .then(() => {
                sinon.assert.calledWith(updateStub, assets[0].filename, assets[0].className);
            });
        });
    });
});
