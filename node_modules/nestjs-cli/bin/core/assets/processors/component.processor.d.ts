import { Processor } from '../../../common/asset/interfaces/processor.interface';
export declare class ComponentProcessor implements Processor {
    private _name;
    private _extension;
    private _generator;
    private _updater;
    private _assets;
    constructor(_name: string, _extension: string);
    process(): Promise<void>;
    private buildAssets();
    private buildClassAsset(className, filename);
    private buildTestAsset(className, filename);
    private generate();
    private updateModule();
}
