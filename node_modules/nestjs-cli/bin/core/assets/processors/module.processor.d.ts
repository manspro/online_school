import { Processor } from '../../../common/asset/interfaces/processor.interface';
export declare class ModuleProcessor implements Processor {
    private _name;
    private _extension;
    private _generator;
    private _assets;
    constructor(_name: string, _extension: string);
    process(): Promise<void>;
    private buildAssets();
    private generate();
}
