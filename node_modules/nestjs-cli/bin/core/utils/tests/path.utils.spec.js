"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const chai_1 = require("chai");
const path_utils_1 = require("../path.utils");
const path = require("path");
describe('PathUtils', () => {
    it('should exist', () => {
        chai_1.expect(path_utils_1.PathUtils).to.exist;
    });
    describe('#relative()', () => {
        it('should return the relative path in the same directory', () => {
            const dirname = 'path/to/file';
            const origin = path.resolve(dirname, 'origin.ext');
            const destination = path.resolve(dirname, 'destination.ext');
            chai_1.expect(path_utils_1.PathUtils.relative(origin, destination)).to.be.equal('./destination.ext');
        });
        it('should return the relative path with origin in a parent directory', () => {
            const origin = path.resolve('path/to/file', 'origin.ext');
            const destination = path.resolve('path/to/file/sub-dir', 'destination.ext');
            chai_1.expect(path_utils_1.PathUtils.relative(origin, destination)).to.be.equal('./sub-dir/destination.ext');
        });
    });
});
