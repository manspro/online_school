"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const child_process = require("child_process");
const sinon = require("sinon");
const chai_1 = require("chai");
const git_utils_1 = require("../git.utils");
describe('GitUtils', () => {
    let sandbox;
    beforeEach(() => sandbox = sinon.sandbox.create());
    afterEach(() => sandbox.restore());
    let execStub;
    beforeEach(() => {
        execStub = sandbox.stub(child_process, 'exec').callsFake((command, callback) => callback());
    });
    it('should call the git system command with the expected inputs', () => {
        return git_utils_1.GitUtils.clone('remote', 'destination')
            .then(() => {
            chai_1.expect(execStub.calledWith(`git clone remote destination`));
        });
    });
});
